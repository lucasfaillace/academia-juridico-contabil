import { PageShell } from "@/components/PageShell";
import { courses } from "@/lib/content";
export default function CoursesPage() { return <PageShell><section className="page-hero"><div className="container narrow"><p className="eyebrow">Cursos</p><h1>Formação profissional</h1><p>Cursos voltados a profissionais do Direito e da Contabilidade que desejam ampliar o conhecimento na área complementar.</p></div></section><section className="section"><div className="container"><div className="course-list">{courses.map((course) => <article key={course.title}><div className="course-number">Em preparação</div><div><h2>{course.title}</h2><p>{course.summary}</p><p><strong>Público:</strong> {course.audience}</p><button className="button secondary" disabled>Disponibilidade a informar</button></div></article>)}</div></div></section></PageShell>; }
