/**
 * ROTA REMOVIDA: Login administrativo legado
 * 
 * Esta rota foi substituída por /api/auth/login e não estava em uso.
 * Sua presença causava erro TypeScript: NextRequest.ip não existe
 * 
 * O login administrativo funciona via:
 * - GET /admin/login (página de login)
 * - POST /api/auth/login (autenticação)
 * 
 * Se precisar recuperar o código legado, consulte o Git history.
 */
